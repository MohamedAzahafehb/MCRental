<Window x:Class="MCRental_Client.Windows.ReserveerWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MCRental_Client.Windows"
        mc:Ignorable="d"
        Title="ReserveerWin" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- Titel -->
            <RowDefinition Height="Auto"/>   <!-- Auto-info -->
            <RowDefinition Height="Auto"/>   <!-- Datums -->
            <RowDefinition Height="Auto"/>   <!-- Prijs -->
            <RowDefinition Height="*" />     <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- Titel -->
        <TextBlock Text="Reserveer" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Grid.Row="0" 
                   Margin="0,0,0,20"/>

        <!-- Auto info -->
        <StackPanel Orientation="Vertical" Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="{Binding Merk}" FontSize="16" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding Model}" FontSize="16"/>
            <TextBlock Text="{Binding Type}" FontSize="16"/>
        </StackPanel>

        <!-- Datums -->
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Begindatum:" FontWeight="Bold"/>
                <DatePicker SelectedDate="{Binding StartDatum, Mode=TwoWay}" />
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="Einddatum:" FontWeight="Bold"/>
                <DatePicker SelectedDate="{Binding EindDatum, Mode=TwoWay}" />
            </StackPanel>
        </Grid>

        <!-- Prijs -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Dagprijs: " FontWeight="Bold"/>
            <TextBlock Text="{Binding DagPrijs, StringFormat=€{0:F2}}"/>
        </StackPanel>
        
        <!-- Knoppen -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Bevestig" Width="120" Height="35"/>
            <Button Content="Annuleer" Width="120" Height="35"/>
        </StackPanel>

    </Grid>
</Window>
