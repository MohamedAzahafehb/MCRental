<Page x:Class="MCRental_Client.Views.ReservatiesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MCRental_Client.Views"
      mc:Ignorable="d"
      Title="ReservatiesView" Height="403" Width="1124">

    <Grid>
        <DataGrid x:Name="dgReservaties" Height="388" Width="1110" VerticalAlignment="Top" HorizontalAlignment="Left" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="StartDatum" Binding="{Binding StartDatum}" Width="100"/>
                <DataGridTextColumn Header="EindDatum" Binding="{Binding EindDatum}" Width="200"/>
                <DataGridTextColumn Header="KlantId" Binding="{Binding KlantId}" Width="200"/>
                <DataGridTextColumn Header="AutoId" Binding="{Binding AutoId}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
