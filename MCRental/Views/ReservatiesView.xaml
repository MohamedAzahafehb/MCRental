<Page x:Class="MCRental_Client.Views.ReservatiesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MCRental_Client.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ReservatiesView">

    <Grid>
        <ListView ItemsSource="{Binding Steden}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="StartDatum" DisplayMemberBinding="{Binding StartDatum}" Width="100"/>
                    <GridViewColumn Header="EindDatum" DisplayMemberBinding="{Binding EindDatum}" Width="200"/>
                    <GridViewColumn Header="KlantId" DisplayMemberBinding="{Binding KlantId}" Width="200"/>
                    <GridViewColumn Header="AutoId" DisplayMemberBinding="{Binding AutoId}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
