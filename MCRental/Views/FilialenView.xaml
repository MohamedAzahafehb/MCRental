<Page x:Class="MCRental_Client.Views.FilialenView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MCRental_Client.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FilialenView">

    <Grid>
        <DataGrid x:Name="dgFilialen" Height="400" Width="1110" VerticalAlignment="Top" HorizontalAlignment="Left" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Naam" Binding="{Binding Naam}" Width="100"/>
                <DataGridTextColumn Header="Adres" Binding="{Binding Adres}" Width="200"/>
                <DataGridTextColumn Header="Telefoon" Binding="{Binding Telefoon}" Width="200"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="StadId" Binding="{Binding StadId}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
