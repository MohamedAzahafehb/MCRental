<Page x:Class="MCRental_Client.Views.StedenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MCRental_Client.Views"
        mc:Ignorable="d"
        Title="StedenView" Height="450" Width="800">
    <Grid>
        <ListView ItemsSource="{Binding Steden}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Postcode" DisplayMemberBinding="{Binding Postcode}" Width="100"/>
                    <GridViewColumn Header="Naam" DisplayMemberBinding="{Binding Naam}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
