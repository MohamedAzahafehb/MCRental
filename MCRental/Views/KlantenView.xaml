<Page x:Class="MCRental_Client.Views.KlantenView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MCRental_Client.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="KlantenView">

    <Grid>
        <ListView ItemsSource="{Binding Steden}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Voornaam" DisplayMemberBinding="{Binding Voornaam}" Width="100"/>
                    <GridViewColumn Header="Achternaam" DisplayMemberBinding="{Binding Achternaam}" Width="200"/>
                    <GridViewColumn Header="Telefoon" DisplayMemberBinding="{Binding Telefoon}" Width="200"/>
                    <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" Width="200"/>
                    <GridViewColumn Header="GeboorteDatum" DisplayMemberBinding="{Binding GeboorteDatum}" Width="200"/>
                    <GridViewColumn Header="Adres" DisplayMemberBinding="{Binding Adres}" Width="200"/>
                    <GridViewColumn Header="StadId" DisplayMemberBinding="{Binding StadId}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
